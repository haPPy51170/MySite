diff --git a/static/css/layout/background.css b/static/css/layout/background.css
index 1d9ad70..00d457d 100644
--- a/static/css/layout/background.css
+++ b/static/css/layout/background.css
@@ -12,6 +12,12 @@ BACKGROUND
 
     overflow:hidden;
 
+    transition:transform .4s ease-out;
+
+    will-change:transform;
+
+    transform-origin:center center;
+
     background:
     radial-gradient(circle at top,var(--primary-a18) 0%,transparent 45%),
     radial-gradient(circle at bottom,rgba(55,224,212,.09) 0%,transparent 40%),
diff --git a/static/css/pages/home.css b/static/css/pages/home.css
index d832e96..2f3439c 100644
--- a/static/css/pages/home.css
+++ b/static/css/pages/home.css
@@ -231,8 +231,6 @@ HERO RIGHT
 
     box-shadow:
 
-    0 0 80px var(--primary-a18),
-
     inset 0 0 20px rgba(255,255,255,.04);
 
 }
diff --git a/static/js/Rapp.js b/static/js/Rapp.js
index 7e34307..9ebb79c 100644
--- a/static/js/Rapp.js
+++ b/static/js/Rapp.js
@@ -94,6 +94,28 @@ window.addEventListener("scroll", () => {
 });
 
 
+/*==============================
+BACKGROUND ORBIT ON SCROLL
+==============================*/
+
+const bgLayer = document.querySelector(".background");
+
+window.addEventListener("scroll", () => {
+
+    if (!bgLayer) return;
+
+    const total = document.documentElement.scrollHeight - window.innerHeight;
+
+    const percent = window.scrollY / total;
+
+    // از صدر تا آخر صفحه، دقیقاً دو دور کامل (۷۲۰ درجه) می‌چرخه
+    const angle = percent * 720;
+
+    bgLayer.style.transform = `rotate(${angle}deg)`;
+
+});
+
+
 /*==============================
 HEADER SCROLL
 ==============================*/
