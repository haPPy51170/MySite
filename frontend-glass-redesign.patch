diff --git a/static/css/base/global.css b/static/css/base/global.css
index 11f4b02..ba36f57 100644
--- a/static/css/base/global.css
+++ b/static/css/base/global.css
@@ -10,6 +10,14 @@ GLOBAL
 
 }
 
+main{
+
+    padding-top:112px;
+
+    flex:1 0 auto;
+
+}
+
 section {
 
     padding:130px 0;
diff --git a/static/css/base/reset.css b/static/css/base/reset.css
index 8556f55..4bf235d 100644
--- a/static/css/base/reset.css
+++ b/static/css/base/reset.css
@@ -17,6 +17,12 @@ html{
 
 }
 
+html,body{
+
+    height:100%;
+
+}
+
 body{
 
     font-family:"Vazirmatn",sans-serif;
@@ -31,6 +37,12 @@ body{
 
     line-height:1.8;
 
+    display:flex;
+
+    flex-direction:column;
+
+    min-height:100vh;
+
 }
 
 img{
diff --git a/static/css/base/root.css b/static/css/base/root.css
index 589ca05..ecf54a8 100644
--- a/static/css/base/root.css
+++ b/static/css/base/root.css
@@ -4,32 +4,48 @@ ROOT
 
 :root{
 
-    --bg:#09090b;
-    --bg-light:#101018;
+    /* پایه‌ی تیره - خنثی‌تر و عمیق‌تر */
+    --bg:#08080c;
+    --bg-light:#0c0c14;
 
-    --surface:#151520;
-    --surface-light:#1d1d2b;
+    /* سطوح شیشه‌ای */
+    --surface:rgba(255,255,255,.045);
+    --surface-light:rgba(255,255,255,.075);
+    --surface-solid:#12121c;
 
-    --primary:#8b5cf6;
-    --primary-light:#a855f7;
+    /* هویت بنفش - نسخه‌ی اصلاح‌شده */
+    --primary:#8b6cf7;
+    --primary-light:#b39dfb;
+    --primary-glow:rgba(139,108,247,.45);
 
-    --secondary:#facc15;
+    /* اکسنت سرد - برای حس گلسی مدرن */
+    --accent:#37e0d4;
+    --accent-light:#6ff0e6;
 
-    --text:#f8fafc;
-    --muted:#9ca3af;
+    /* اکسنت گرم - نسخه‌ی ملایم‌شده‌ی زرد قبلی */
+    --secondary:#eab94d;
 
-    --border:rgba(255,255,255,.08);
+    --text:#f4f5fa;
+    --muted:#9298ac;
+
+    /* حاشیه‌ها و لایه‌های شیشه */
+    --border:rgba(255,255,255,.09);
+    --glass-border:rgba(255,255,255,.12);
+    --glass-highlight:rgba(255,255,255,.22);
 
     --success:#22c55e;
     --danger:#ef4444;
 
-    --shadow:0 20px 60px rgba(0,0,0,.35);
+    --shadow:0 20px 60px rgba(0,0,0,.4);
+    --shadow-glass:0 8px 32px rgba(0,0,0,.3);
 
-    --radius:22px;
+    --radius:26px;
+    --radius-sm:18px;
+    --radius-lg:38px;
 
-    --transition:.35s ease;
+    --blur:22px;
+    --transition:.35s cubic-bezier(.4,0,.2,1);
 
     --container:1240px;
 
 }
-
diff --git a/static/css/components/buttons.css b/static/css/components/buttons.css
index 1f2b51c..df9fbdf 100644
--- a/static/css/components/buttons.css
+++ b/static/css/components/buttons.css
@@ -27,7 +27,7 @@ BUTTONS
 
     box-shadow:
 
-    0 0 45px rgba(139,92,246,.35);
+    0 0 45px rgba(139,108,247,.35);
 
 }
 
@@ -37,7 +37,7 @@ BUTTONS
 
     box-shadow:
 
-    0 15px 40px rgba(139,92,246,.45);
+    0 15px 40px rgba(139,108,247,.45);
 
 }
 
diff --git a/static/css/components/forms.css b/static/css/components/forms.css
index 05d82f1..890a99f 100644
--- a/static/css/components/forms.css
+++ b/static/css/components/forms.css
@@ -20,7 +20,7 @@ FORMS & INPUTS
 .input-group input:focus,
 .input-group textarea:focus {
     border-color: var(--primary);
-    box-shadow: 0 0 25px rgba(139, 92, 246, .18);
+    box-shadow: 0 0 25px rgba(139, 108, 247, .18);
 }
 
 /* Form Errors */
diff --git a/static/css/layout/footer.css b/static/css/layout/footer.css
index 70a602f..3c69021 100644
--- a/static/css/layout/footer.css
+++ b/static/css/layout/footer.css
@@ -4,13 +4,48 @@ FOOTER
 
 .footer{
 
-    padding:80px 0 35px;
+    margin:120px 16px 16px;
 
-    margin-top:120px;
+    padding:60px 0 35px;
 
-    border-top:1px solid rgba(255,255,255,.08);
+    border-radius:var(--radius-lg) var(--radius-lg) 0 0;
 
-    background:rgba(255,255,255,.02);
+    background:rgba(15,15,24,.55);
+
+    backdrop-filter:blur(var(--blur)) saturate(150%);
+
+    -webkit-backdrop-filter:blur(var(--blur)) saturate(150%);
+
+    border:1px solid var(--glass-border);
+
+    border-bottom:none;
+
+    position:relative;
+
+    overflow:hidden;
+
+}
+
+.footer::before{
+
+    content:"";
+
+    position:absolute;
+
+    top:0;
+
+    right:20%;
+
+    left:20%;
+
+    height:1px;
+
+    background:linear-gradient(
+        90deg,
+        transparent,
+        var(--glass-highlight) 50%,
+        transparent
+    );
 
 }
 
@@ -40,7 +75,7 @@ FOOTER
 
     display:flex;
     flex-direction:column;
-    gap:14px;
+    gap:12px;
 
 }
 
@@ -58,7 +93,7 @@ FOOTER
 
     padding-top:24px;
 
-    border-top:1px solid rgba(255,255,255,.08);
+    border-top:1px solid var(--border);
 
 }
 
@@ -66,15 +101,17 @@ FOOTER
 
     color:var(--muted);
 
-    transition:.3s;
+    transition:var(--transition);
+
+    width:fit-content;
 
 }
 
 .footer-center a:hover{
 
-    color:var(--secondary);
+    color:var(--accent-light);
 
-    transform:translateX(-8px);
+    transform:translateX(-6px);
 
 }
 
@@ -82,7 +119,7 @@ FOOTER
 
     display:flex;
 
-    gap:18px;
+    gap:14px;
 
     margin-top:35px;
 
@@ -92,9 +129,9 @@ FOOTER
 
 .footer-right a{
 
-    width:52px;
+    width:48px;
 
-    height:52px;
+    height:48px;
 
     display:grid;
 
@@ -102,26 +139,28 @@ FOOTER
 
     border-radius:50%;
 
-    background:rgba(255,255,255,.05);
+    background:var(--surface);
+
+    border:1px solid var(--glass-border);
 
-    border:1px solid rgba(255,255,255,.08);
+    color:var(--text);
 
-    transition:.35s;
+    transition:var(--transition);
 
 }
 
 .footer-right a:hover{
 
     background:linear-gradient(
+        135deg,
+        var(--primary),
+        var(--accent)
+    );
 
-    135deg,
+    color:#0a0a0f;
 
-    var(--primary),
+    transform:translateY(-6px);
 
-    var(--primary-light));
+    box-shadow:0 8px 24px var(--primary-glow);
 
-    color:#fff;
-
-    transform:translateY(-8px) rotate(8deg);
-
-}
\ No newline at end of file
+}
diff --git a/static/css/layout/header.css b/static/css/layout/header.css
index 2d22580..02e798b 100644
--- a/static/css/layout/header.css
+++ b/static/css/layout/header.css
@@ -6,40 +6,96 @@ HEADER
 
     position:fixed;
 
-    top:0;
+    top:16px;
 
     right:0;
 
-    width:100%;
+    left:0;
 
     z-index:9999;
 
-    backdrop-filter:blur(18px);
+    display:flex;
+
+    justify-content:center;
+
+    transition:var(--transition);
+
+}
+
+.header .container{
+
+    height:72px;
+
+    display:flex;
+
+    justify-content:space-between;
+
+    align-items:center;
+
+    width:min(92%,var(--container));
+
+    padding:0 32px 0 26px;
+
+    border-radius:999px;
+
+    background:rgba(18,18,28,.55);
 
-    background:rgba(9,9,11,.55);
+    backdrop-filter:blur(var(--blur)) saturate(160%);
 
-    border-bottom:1px solid var(--border);
+    -webkit-backdrop-filter:blur(var(--blur)) saturate(160%);
+
+    border:1px solid var(--glass-border);
+
+    box-shadow:var(--shadow-glass);
+
+    position:relative;
+
+    overflow:hidden;
 
     transition:var(--transition);
 
 }
 
-.header.scrolled{
+/* لبه‌ی نوری بالای کپسول - حس شیشه زیر نور */
+.header .container::before{
+
+    content:"";
+
+    position:absolute;
+
+    top:0;
+
+    right:14%;
+
+    left:14%;
 
-    background:#11111b;
+    height:1px;
 
-    box-shadow:var(--shadow);
+    background:linear-gradient(
+        90deg,
+        transparent,
+        var(--glass-highlight) 50%,
+        transparent
+    );
 
 }
 
-.header .container{
+.header.scrolled .container{
 
-    height:82px;
+    background:rgba(14,14,22,.74);
 
-    display:flex;
+    border-color:rgba(255,255,255,.16);
 
-    justify-content:space-between;
+    box-shadow:0 14px 45px rgba(0,0,0,.5);
 
-    align-items:center;
+}
 
-}
\ No newline at end of file
+.header .logo{
+
+    font-weight:800;
+
+    font-size:1.2rem;
+
+    letter-spacing:.3px;
+
+}
diff --git a/static/css/layout/navbar.css b/static/css/layout/navbar.css
index d5ab7a3..f2821e0 100644
--- a/static/css/layout/navbar.css
+++ b/static/css/layout/navbar.css
@@ -8,7 +8,7 @@ NAVBAR
 
     align-items:center;
 
-    gap:38px;
+    gap:6px;
 
 }
 
@@ -18,43 +18,57 @@ NAVBAR
 
     font-weight:500;
 
+    font-size:.94rem;
+
     position:relative;
 
+    padding:9px 18px;
+
+    border-radius:999px;
+
     transition:var(--transition);
 
+    display:inline-block;
+
 }
 
 .navbar a:hover{
 
-    color:#fff;
+    color:var(--text);
 
-}
+    background:var(--surface-light);
 
-.navbar a::after{
+}
 
-    content:"";
+.navbar a.active{
 
-    position:absolute;
+    color:var(--text);
 
-    right:0;
+    background:linear-gradient(
+        135deg,
+        var(--primary-glow),
+        rgba(55,224,212,.18)
+    );
 
-    bottom:-8px;
+    border:1px solid var(--glass-border);
 
-    width:0;
+}
 
-    height:2px;
+.nav-login a{
 
-    border-radius:10px;
+    background:linear-gradient(135deg,var(--primary),var(--primary-light));
 
-    background:var(--secondary);
+    color:#fff !important;
 
-    transition:.35s;
+    box-shadow:0 4px 18px var(--primary-glow);
 
 }
 
-.navbar a:hover::after{
+.nav-login a:hover{
+
+    filter:brightness(1.1);
 
-    width:100%;
+    transform:translateY(-1px);
 
 }
 
@@ -80,7 +94,9 @@ NAVBAR
 
     margin:8px 0;
 
-    background:#fff;
+    border-radius:2px;
+
+    background:var(--text);
 
     transition:.3s;
 
@@ -96,4 +112,4 @@ NAVBAR
 
 .menu-btn.active span:nth-child(3){
     transform:translateY(-10px) rotate(-45deg);
-}
\ No newline at end of file
+}
diff --git a/static/css/pages/blog_detail.css b/static/css/pages/blog_detail.css
index 308bc76..373b155 100644
--- a/static/css/pages/blog_detail.css
+++ b/static/css/pages/blog_detail.css
@@ -27,7 +27,7 @@
     transition: .35s;
 
     box-shadow:
-        0 18px 45px rgba(139,92,246,.12);
+        0 18px 45px rgba(139,108,247,.12);
 }
 
 .blog-post-card::before {
@@ -40,7 +40,7 @@
     background:
         linear-gradient(
             135deg,
-            rgba(139,92,246,.08),
+            rgba(139,108,247,.08),
             transparent 60%
         );
 
diff --git a/static/css/pages/home.css b/static/css/pages/home.css
index 9781ada..91c0a08 100644
--- a/static/css/pages/home.css
+++ b/static/css/pages/home.css
@@ -16,7 +16,7 @@ LOGO
 
 .logo span{
 
-    color:var(--secondary);
+    color:var(--primary-light);
 
 }
 
@@ -177,7 +177,7 @@ HERO STATS
 
     box-shadow:
 
-    0 10px 35px rgba(139,92,246,.25);
+    0 10px 35px rgba(139,108,247,.25);
 
 }
 
@@ -275,7 +275,7 @@ HERO RIGHT
 
     overflow:hidden;
 
-    border-radius:22px;
+    border-radius:26px;
 
     border:2px solid rgba(255,255,255,.08);
 
@@ -283,7 +283,7 @@ HERO RIGHT
 
     box-shadow:
 
-    0 0 80px rgba(139,92,246,.20),
+    0 0 80px rgba(139,108,247,.20),
 
     inset 0 0 20px rgba(255,255,255,.04);
 
@@ -474,7 +474,7 @@ ABOUT
 
     box-shadow:
 
-    0 20px 45px rgba(139,92,246,.18);
+    0 20px 45px rgba(139,108,247,.18);
 
 }
 
@@ -492,7 +492,7 @@ ABOUT
 
     circle,
 
-    rgba(139,92,246,.35),
+    rgba(139,108,247,.35),
 
     transparent 70%);
 
@@ -630,7 +630,7 @@ PROJECTS
 
     overflow:hidden;
 
-    border-radius:24px;
+    border-radius:var(--radius);
 
     background:rgba(255,255,255,.04);
 
@@ -648,7 +648,7 @@ PROJECTS
 
     box-shadow:
 
-    0 18px 45px rgba(139,92,246,.20);
+    0 18px 45px rgba(139,108,247,.20);
 
 }
 
@@ -761,7 +761,7 @@ BLOG
 
     padding:35px;
 
-    border-radius:24px;
+    border-radius:var(--radius);
 
     background:rgba(255,255,255,.04);
 
@@ -783,7 +783,7 @@ BLOG
 
     box-shadow:
 
-    0 18px 40px rgba(139,92,246,.18);
+    0 18px 40px rgba(139,108,247,.18);
 
 }
 
@@ -801,7 +801,7 @@ BLOG
 
     135deg,
 
-    rgba(139,92,246,.08),
+    rgba(139,108,247,.08),
 
     transparent 60%);
 
@@ -880,7 +880,7 @@ CONTACT
 
     padding:25px;
 
-    border-radius:22px;
+    border-radius:var(--radius);
 
     background:rgba(255,255,255,.04);
 
@@ -946,7 +946,7 @@ CONTACT
 
     box-shadow:
 
-    0 0 25px rgba(139,92,246,.18);
+    0 0 25px rgba(139,108,247,.18);
 
 }
 
@@ -1046,9 +1046,9 @@ REVEAL ANIMATION
 
 .custom-select-trigger:hover,
 .custom-select.open .custom-select-trigger {
-    border-color: rgba(139, 92, 246, 0.45);
-    background: rgba(139, 92, 246, 0.08);
-    box-shadow: 0 10px 35px rgba(139, 92, 246, 0.18);
+    border-color: rgba(139, 108, 247, 0.45);
+    background: rgba(139, 108, 247, 0.08);
+    box-shadow: 0 10px 35px rgba(139, 108, 247, 0.18);
 }
 
 .custom-select-value {
@@ -1085,17 +1085,17 @@ REVEAL ANIMATION
 
     padding: 8px;
 
-    border-radius: 20px;
+    border-radius: 24px;
 
     background: rgba(16, 16, 24, 0.92);
-    border: 1px solid rgba(139, 92, 246, 0.18);
+    border: 1px solid rgba(139, 108, 247, 0.18);
 
     backdrop-filter: blur(22px);
     -webkit-backdrop-filter: blur(22px);
 
     box-shadow:
         0 20px 50px rgba(0, 0, 0, 0.4),
-        0 0 30px rgba(139, 92, 246, 0.08);
+        0 0 30px rgba(139, 108, 247, 0.08);
 
     opacity: 0;
     visibility: hidden;
@@ -1126,7 +1126,7 @@ REVEAL ANIMATION
     padding: 12px 14px;
 
     border: 0;
-    border-radius: 13px;
+    border-radius: 16px;
 
     background: transparent;
 
@@ -1149,12 +1149,12 @@ REVEAL ANIMATION
 .custom-select-option:focus {
     outline: none;
 
-    background: rgba(139, 92, 246, 0.12);
+    background: rgba(139, 108, 247, 0.12);
     color: var(--text);
 }
 
 .custom-select-option.selected {
-    background: rgba(139, 92, 246, 0.18);
+    background: rgba(139, 108, 247, 0.18);
     color: var(--primary-light);
     font-weight: 700;
 }
@@ -1167,8 +1167,8 @@ REVEAL ANIMATION
 
     border-radius: 50px;
 
-    background: rgba(139, 92, 246, 0.1);
-    border: 1px solid rgba(139, 92, 246, 0.2);
+    background: rgba(139, 108, 247, 0.1);
+    border: 1px solid rgba(139, 108, 247, 0.2);
 
     color: var(--primary-light);
 
diff --git a/static/css/pages/login.css b/static/css/pages/login.css
index 43b8808..209966d 100644
--- a/static/css/pages/login.css
+++ b/static/css/pages/login.css
@@ -124,7 +124,7 @@ LOGIN PAGE
 
     box-shadow:
 
-    0 0 20px rgba(139,92,246,.25);
+    0 0 20px rgba(139,108,247,.25);
 
 }
 
@@ -221,7 +221,7 @@ LOGIN OPTIONS
 }
 
 .navbar .nav-login{
-    margin-right: 28px;
+    margin-right: 6px;
 }
 
 .navbar .nav-login a{
diff --git a/static/css/style.css b/static/css/style.css
index 059e062..962e4f4 100644
--- a/static/css/style.css
+++ b/static/css/style.css
@@ -30,6 +30,7 @@ COMPONENTS
 ==============================*/
 
 @import url("components/buttons.css");
+@import url("components/forms.css");
 @import url("components/progress-bar.css");
 @import url("components/typing.css");
 
